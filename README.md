# 3mui

```
apt update -y && apt install -y dnsutils curl && FILE="3mui.sh" && curl -sSL -H "Accept: application/vnd.github.v3.raw" -o $FILE "https://api.github.com/repos/xtonly/3mui/contents/3mui.sh?ref=main" && chmod +x $FILE && ./$FILE
```
